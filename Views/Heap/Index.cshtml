<style>
    .heap-node-circle {
        transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .node-pulse {
        animation: pulse-gold 1.5s infinite;
    }
    .node-swap {
        animation: swap-green 0.8s ease-in-out;
    }
    @@keyframes pulse-gold {
        0% { filter: drop-shadow(0 0 2px #fbbf24); transform: scale(1); }
        50% { filter: drop-shadow(0 0 10px #fbbf24); transform: scale(1.05); }
        100% { filter: drop-shadow(0 0 2px #fbbf24); transform: scale(1); }
    }
    @@keyframes swap-green {
        0% { transform: scale(1); filter: brightness(1); }
        50% { transform: scale(1.2); filter: brightness(1.5); stroke: #4ade80; }
        100% { transform: scale(1); filter: brightness(1); }
    }
    .array-box {
        transition: transform 0.3s ease;
    }
    .array-swap {
        animation: slide-up 0.5s ease;
    }
    @@keyframes slide-up {
        0% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
        100% { transform: translateY(0); }
    }
</style>

<div class="space-y-6 animate-fade-in-up">
    <div class="glass-panel algo-header">
        <div class="header-left">
            <h1 style="font-size: 1.875rem; font-weight: bold; background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; margin-bottom: 0.25rem;">
                Max Heap Construction
            </h1>
            <p style="color: #64748b; font-size: 0.875rem;">Building a Complete Binary Tree where every parent is greater than its children.</p>
        </div>
        <div class="header-right" style="display: flex; align-items: center; gap: 1.5rem;">
             <div style="text-align: right;">
                <div id="timerDisplay" style="font-family: monospace; font-size: 1.5rem; font-weight: bold; color: #fbbf24; line-height: 1;">00:00</div>
                <div style="font-size: 0.875rem; color: #94a3b8;">Heap Visualizer</div>
            </div>
            <button id="resetBtn" class="btn btn-secondary">Reset</button>
        </div>
    </div>

    <div class="puzzle-grid">
        <div class="glass-panel" style="padding: 2rem; border-radius: 1rem; min-height: 500px;">
            <div id="mapContainer" style="position: relative; width: 100%; height: auto; min-height: 400px; background: rgba(15, 23, 41, 0.5); border-radius: 0.5rem; overflow: hidden; display: flex; flex-direction: column; align-items: center; justify-content: center;">
                <svg id="heapSvg" viewBox="0 0 800 400" style="width: 100%; height: 100%; pointer-events: auto;"></svg>
                
                <div id="resultOverlay" style="position: absolute; inset: 0; display: none; align-items: center; justify-content: center; pointer-events: none; z-index: 10;">
                    <div style="background: rgba(15, 23, 42, 0.95); padding: 2.5rem; border-radius: 1rem; border: 1px solid #334155; text-align: center; backdrop-filter: blur(8px); min-width: 320px;">
                        <h2 style="font-size: 1.75rem; font-weight: bold; color: #fbbf24; margin-bottom: 1.5rem;">🎉 Heap Ready!</h2>
                        <div style="display: flex; flex-direction: column; gap: 0.75rem; margin-bottom: 2rem;">
                            <div style="color: #cbd5e1; font-size: 1.1rem;">Time: <span id="resultTime" style="color: #4ade80; font-weight: bold;">00:00</span></div>
                            <div style="color: #cbd5e1; font-size: 1.1rem;">Score: <span id="finalScore" style="color: #fbbf24; font-weight: bold;">0</span></div>
                        </div>
                        <button id="closeResultBtn" class="btn btn-primary" style="pointer-events: auto; background: #fbbf24; border: none; color: #000;">Close</button>
                    </div>
                </div>
            </div>

            <!-- Array Visualization Area -->
            <div style="margin-top: 2rem; padding: 1.5rem; background: rgba(15, 23, 42, 0.4); border: 1px solid #1e293b; border-radius: 0.75rem;">
                <h4 style="color: #94a3b8; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 1rem;">Array Representation</h4>
                <div id="arrayContainer" style="display: flex; gap: 0.5rem; flex-wrap: wrap; justify-content: center;">
                    <span style="color: #64748b; font-style: italic;">Generate an array to begin</span>
                </div>
            </div>
        </div>

        <div class="glass-panel" style="padding: 1.5rem; border-radius: 1rem; display: flex; flex-direction: column; gap: 1.5rem;">
            <div id="inputSection">
                <h3 style="font-size: 1.125rem; font-weight: 600; color: white; margin-bottom: 1rem;">Heap Input</h3>
                
                <div style="display: flex; gap: 0.5rem; margin-bottom: 1.5rem;">
                    <button id="tabRandom" class="btn btn-sm btn-primary">Random</button>
                    <button id="tabManual" class="btn btn-sm btn-secondary">Manual</button>
                </div>

                <div id="randomInputGroup">
                    <label style="display: block; color: #94a3b8; font-size: 0.875rem; margin-bottom: 0.5rem;">Number of Elements (5-15)</label>
                    <input type="number" id="nodeCountInput" value="10" min="5" max="15" class="form-control" style="width: 100%; background: #1e293b; border: 1px solid #334155; color: white; padding: 0.5rem; border-radius: 0.25rem; margin-bottom: 1.5rem;">
                    <button id="generateBtn" class="btn btn-primary" style="width: 100%; background: #fbbf24; color: #000; border: none;">Generate Array</button>
                </div>

                <div id="manualInputGroup" style="display: none;">
                    <label style="display: block; color: #94a3b8; font-size: 0.875rem; margin-bottom: 0.5rem;">Enter Numbers (Comma separated)</label>
                    <textarea id="manualArrayInput" rows="3" class="form-control" style="width: 100%; background: #1e293b; border: 1px solid #334155; color: white; padding: 0.5rem; border-radius: 0.25rem; margin-bottom: 1.5rem; font-family: monospace; resize: none;">45, 20, 10, 31, 7, 11, 13, 2, 8, 15</textarea>
                    <button id="generateManualBtn" class="btn btn-primary" style="width: 100%; background: #fbbf24; color: #000; border: none;">Load Array</button>
                </div>
            </div>

            <div id="actionSection" style="display: none; flex-direction: column; gap: 1rem;">
                <h3 style="font-size: 1.125rem; font-weight: 600; color: white; margin-bottom: 0.5rem;">Controls</h3>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; margin-bottom: 1.5rem;">
                    <button id="solveBtn" class="btn btn-primary" style="background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%); border: none; color: #000;">Build Max Heap (Auto)</button>
                    <button id="manualSolveBtn" class="btn btn-secondary" style="border-color: #fbbf24; color: #fbbf24;">Manual Solve</button>
                </div>
                 <div id="stepControls" style="display: none; gap: 0.5rem; justify-content: center; margin-top: 0.5rem;">
                     <button id="prevBtn" class="btn btn-sm btn-secondary" style="padding: 0.25rem 0.75rem; border-color: #fbbf24; color: #fbbf24;">&lt;</button>
                     <button id="pauseBtn" class="btn btn-sm btn-secondary" style="padding: 0.25rem 0.75rem; border-color: #fbbf24; color: #fbbf24;">||</button>
                     <button id="nextBtn" class="btn btn-sm btn-secondary" style="padding: 0.25rem 0.75rem; border-color: #fbbf24; color: #fbbf24;">&gt;</button>
                 </div>
            </div>

            <div style="border-top: 1px solid rgba(255, 255, 255, 0.1); padding-top: 1rem;">
                 <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.75rem;">
                    <h3 style="font-size: 1.125rem; font-weight: 600; color: white; margin: 0;">Status</h3>
                    <div style="font-size: 0.875rem; color: #fbbf24;">Score: <span id="currentScoreDisplay">0</span></div>
                 </div>
                 <div id="statusText" style="margin-bottom: 1rem; font-size: 0.875rem; color: #cbd5e1; min-height: 4rem; background: rgba(0,0,0,0.2); padding: 0.75rem; border-radius: 0.5rem; line-height: 1.5;">Ready to build heap.</div>
            </div>

            <div style="border-top: 1px solid rgba(255, 255, 255, 0.1); padding-top: 1.5rem; margin-top: 1rem;">
                <h3 style="font-size: 1.125rem; font-weight: bold; color: white; margin-bottom: 1rem;">Complexity Analysis</h3>
                <div style="margin-bottom: 1.5rem;">
                    <p style="color: #94a3b8; font-size: 0.75rem; margin-bottom: 0.25rem;">Time Complexity:</p>
                    <div style="background: rgba(251, 191, 36, 0.15); border: 1px solid rgba(251, 191, 36, 0.4); color: #fbbf24; padding: 0.25rem 0.75rem; border-radius: 0.375rem; display: inline-block; font-family: monospace; font-size: 0.875rem; margin-bottom: 1rem;">
                        O(N) - Build Heap
                    </div>
                    
                    <p style="color: #94a3b8; font-size: 0.75rem; margin-bottom: 0.25rem; margin-top: 0.5rem;">Space Complexity:</p>
                    <div style="background: rgba(251, 191, 36, 0.15); border: 1px solid rgba(251, 191, 36, 0.4); color: #fbbf24; padding: 0.25rem 0.75rem; border-radius: 0.375rem; display: inline-block; font-family: monospace; font-size: 0.875rem; margin-bottom: 1rem;">
                        O(log N) - Recursive Stack
                    </div>
                </div>

                <h3 style="font-size: 1.125rem; font-weight: bold; color: white; margin-bottom: 0.5rem;">How it works</h3>
                <p style="color: #94a3b8; font-size: 0.825rem; margin-bottom: 1rem; line-height: 1.5;">
                    The <b>Build Heap</b> algorithm starts from the last non-leaf node and calls <b>Heapify</b> on each node moving upwards. Heapify ensures that the larger of the two children is swapped with the parent if necessary, bubbling the violation down until the property holds.
                </p>

                <h3 style="font-size: 1.125rem; font-weight: bold; color: white; margin-bottom: 0.5rem;">Heapify Pseudocode</h3>
                <div style="background: #0f172a; padding: 1rem; border-radius: 0.5rem; border: 1px solid #1e293b; font-family: monospace; font-size: 0.8rem; color: #fbbf24; line-height: 1.5;">
                    <span style="color: #f59e0b;">MaxHeapify(A, i)</span>:<br>
                    &nbsp;&nbsp;l = Left(i), r = Right(i)<br>
                    &nbsp;&nbsp;<span style="color: #94a3b8;">if</span> l <= size <span style="color: #94a3b8;">and</span> A[l] > A[i]:<br>
                    &nbsp;&nbsp;&nbsp;&nbsp;largest = l<br>
                    &nbsp;&nbsp;<span style="color: #94a3b8;">else</span> largest = i<br>
                    &nbsp;&nbsp;<span style="color: #94a3b8;">if</span> r <= size <span style="color: #94a3b8;">and</span> A[r] > A[largest]:<br>
                    &nbsp;&nbsp;&nbsp;&nbsp;largest = r<br>
                    &nbsp;&nbsp;<span style="color: #94a3b8;">if</span> largest != i:<br>
                    &nbsp;&nbsp;&nbsp;&nbsp;swap(A[i], A[largest])<br>
                    &nbsp;&nbsp;&nbsp;&nbsp;<span style="color: #f59e0b;">MaxHeapify(A, largest)</span>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts { <script src="~/js/heap.js"></script> }
